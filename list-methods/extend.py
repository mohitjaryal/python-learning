# .extend() method in a list is used to add multiple elements (from another iterable) to the end of a

colors = ['voilet','indigo','blue']

rainbow = ['green', 'yellow', 'orange', 'red']

colors.extend(rainbow)

print(colors)